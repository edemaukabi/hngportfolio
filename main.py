# This is a simple application that prints my name to the console when run
"""To run this program, open the cloned or downloaded folder in your code editor, cd into the twoweek 
folder and run the command 'python3 main.py' without the quote
Note: The program uses formated f string literal which is a python3 upgrade, so running python main.py will fail.
Use python3"""

def my_name(name):
    display_name = f"my name is {name}"
    print(display_name)

my_name('Edema Ukabi')
